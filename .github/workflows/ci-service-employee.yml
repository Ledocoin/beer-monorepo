name: Build — service-employee
on:
  push:
    paths:
      - "service-employee/**"
      - ".github/workflows/build-service-employee.yml"
  pull_request:
    paths:
      - "service-employee/**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: service-employee
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: gradle

      - name: Make gradlew executable
        run: sed -i 's/\r$//' gradlew && chmod +x gradlew

      - name: Build & Test
        run: ./gradlew clean build --no-daemon